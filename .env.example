# Copy to `.env.local` and fill in values. Only `NEXT_PUBLIC_*` keys are exposed to the browser.

# Supabase (when integrated)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Paystack (when integrated)
# NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=
# PAYSTACK_SECRET_KEY=
